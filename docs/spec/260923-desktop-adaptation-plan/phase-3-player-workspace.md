# P3：播放工作区、歌词与宽队列

[返回 spec](README.md) · [总体设计](desktop-adaptation-plan.md) · [验收](acceptance.md)

状态：首轮实现中。封面固定、歌词/队列右侧切换、封面动态背景、桌面常驻播放条和与进度条统一的细音量 scrubber 已落代码；播放工作区会在宽/高空间不足时切为紧凑歌曲摘要栏，让歌词/队列占用主体区域。封面动态背景默认开启，设置可持久化关闭；关闭时迷你播放器、队列、桌面工作区和手机全屏播放器使用当前 Echo 主题色。桌面队列支持单击选择、双击播放、明确播放按钮、可见鼠标拖动把手、序号、按宽度显示的专辑/时长、焦点内 Enter/Delete、按稳定 entry ID 执行的右键操作，以及标题栏“定位当前”按钮。若拖动期间队列 revision 或随机模式改变，会取消本次排序并用提示告知重新拖动，避免按旧 index 错排。工作区首次打开后保持挂载；歌词/队列面板按需挂载并在切换时保留内部滚动与选中状态，工作区收起到浏览页后也不卸载，隐藏时关闭 ticker、焦点和鼠标输入。Linux/Windows 桌面播放工作区可请求系统全屏，Esc 先退出全屏；全屏时继续使用同一歌词/队列内容并保留 Echo 自绘窗口栏。窗口状态服务忽略全屏分辨率，退出全屏后再保存正常窗口尺寸；Android 不显示原生全屏入口。手机仍保留单击播放/长按排序。全局 Space、Ctrl+F、Ctrl+方向键与 Esc 也已接入桌面 shell，文本/滑杆会优先处理按键。远端当前项定位按锚点缓存的已渲染行索引逐步校正；队列滚动会在帧后清理离屏锚点，采样不再对每个行 key 线性扫描队列。5000 首定位、专辑断点和本轮新增用例未执行，实际长队列性能、键盘/全屏操作与 Ubuntu 不同窗口尺寸验收仍待完成。前置：P1 命令/快照契约、P2 shell 与常驻播放条已建立。后续：P4 联调系统入口，P5 验收性能和多平台。

## 目标与边界

按参考图完成窗口内播放工作区：左侧封面/歌曲信息，右侧歌词或宽队列。进入时收起资料库侧栏，返回时恢复浏览位置。底部播放条始终由同一个 shell 实例承载。

第一版实现单项选择、播放、定位当前、拖拽、移除和清空后续。队列搜索、多选批处理、删除撤销、浏览时停靠队列及悬浮歌词列入后续，不为它们新增第一版状态模型。

## 界面状态与操作

| 当前状态 | 操作 | 结果 |
| --- | --- | --- |
| browse | 点击底部封面/歌词入口 | 进入 player-lyrics，保留浏览分支、筛选与滚动 |
| browse | 点击队列入口 | 进入 player-queue |
| player-lyrics | 点击队列入口 | 仅右侧换为队列，封面和底栏保持位置 |
| player-queue | 点击歌词入口 | 仅右侧换为歌词 |
| 任一播放工作区 | 再次点击底部当前模式按钮或点击收起 | 返回 browse；工作区内部 tab 点击只选择 tab，不关闭 |
| 任意界面 | Esc | 先关闭当前菜单/弹窗；再退出真正全屏；再收起播放工作区，每次只处理最上层 |
| 无当前曲目 | 点击歌词入口 | 入口禁用或呈现明确空状态；队列入口仍可查看已有队列 |

`browse/player-lyrics/player-queue` 是布局状态，不创建新的音频会话。切换发生在稳定根容器内；隐藏的浏览内容和已打开的播放工作区均保留状态，但停止不可见动画、焦点和多余 ticker。登录失效、切库的行为沿用 P2 的导航/鉴权规则。

## 实施步骤

1. 新建桌面工作区编排，使用共享 cover、track identity、lyrics 与 queue content。通过 mode 状态控制内容，不复用手机全屏页面的整页 route/Hero 飞行动画。
2. 大窗口的封面上限先采用设计建议 320–400 逻辑像素；封面列、歌词阅读列与队列表格分别限宽。尺寸取实际内容约束，不能把播放器按窗口比例整体放大。
3. 空间不足时先降低封面尺寸，再将封面收为顶部歌曲摘要；队列/歌词保留主要空间，底部进度与播放操作始终可用。窗口高度不足时仅内容滚动，控制区保持可访问。
4. 歌词接入现有解析与同步组件，覆盖加载中、无歌词、错误重试、双语、大字体。用户手动滚动后暂停自动跟随，点击“回到当前”恢复；切歌重置到新歌曲，普通 tab 切换不错误重置到开头。
5. 歌词点击仍调用逻辑 seek，加载/转码期间显示真实忙碌状态。远距离连续点击以最后意图为准，不能另写一套寻址/重试算法。
6. 队列按宽度显示序号、封面、歌名/歌手、专辑、时长和动作。优先收起专辑等次要列；保留明确播放、移除或更多入口，不依赖鼠标悬停发现核心操作。
7. 界面选择使用 entry ID，与 currentEntryId 分开；单击选中、双击/Enter 播放按 D2 实现。手机/触控行为按 presenter 保留单击播放。右键菜单对正确条目操作，不改变实际播放位置。
8. 排序把手可见，鼠标拖拽无需先长按。保留 revision 校验；拖拽期间队列已经改变时取消本次排序并提示，不将旧 index 应用到新队列。重复歌曲始终按 entry ID 处理。
9. 定位当前：用户打开队列或主动定位时滚动到真正当前项；播放进度 tick 不拉回用户正在浏览的位置。保留滚动状态，远距离定位验证估算位置与最终对齐。
10. 清空后续与移除遵守既有队列方案。切换随机/循环不得重排用户的选择状态到错误条目；当前项被删除后的播放行为继续由 notifier 决定。
11. 增加应用内 Shortcuts/Actions：Space、Ctrl+F、Ctrl+Left/Right、Esc；列表 Enter 播放，Delete 仅在队列列表获得焦点时移除选中项。输入框、slider、菜单先处理自身按键，slider 上的 Space/方向键不同时触发播放切歌。
12. 选择性启用真正全屏；进入/退出时保存正常窗口状态，队列和歌词继续使用同一组件。全屏时是否隐藏 Echo 窗口栏需在窗口状态验收中单独确定；当前先保证普通窗口的拖动和系统控制。

## 主要文件

`lib/features/player/pages/full_player_page.dart` 保留手机编排；新增桌面工作区文件；复用 `synced_lyrics_view.dart`、`player_scrubber.dart`、P1 队列内容和 `lib/widgets/song_list_item.dart`。P2 底栏的歌词/队列入口切到工作区，不再打开手机 bottom sheet。

## 验证与完成条件

- A05：1654 首实际量级和 5000 首构造队列可滚动、定位当前、排序；重复项/当前队尾/拖拽期间修改不会播错或删错。
- A06：歌词与队列切换不造成封面/底栏跳动或停止播放；收起后回到原浏览位置。工作区重新打开时队列选中项、滚动位置和歌词手动浏览状态仍在。大字体、无歌词与错误态不挤掉控制区。动态封面背景默认开启；关闭后播放器表面使用当前主题颜色，并在重启后保留选择。
- A07/A11：鼠标右键菜单、键盘、触控语义分别验证；右键动作按 entry ID 执行，焦点可见，Esc 层级明确，输入搜索文本不会触发快捷播放。
- A15：队列保持懒构建，进度更新不重建全量行；在 release/profile 中记录连续滚动、切 tab 和缩放的帧耗时，不能用 debug 卡顿代表正式性能。
- 复用扩展 `test/features/player/play_queue_sheet_test.dart`、`synced_lyrics_view_test.dart`、`player_navigation_flow_test.dart`；增加桌面模式切换、焦点、选中与播放分离测试。
- Android 的全屏播放器、MiniPlayer、队列底部面板与 Hero 路径仍通过原有相关测试。

## 实施记录

| 日期 / 提交 | 实际修改与检查 | 结果 / 遗留 |
| --- | --- | --- |
| 2026-09-23 / `f6100044` | 新增桌面封面/信息与歌词或队列右栏；抽出 `CurrentLyricsPanel` 和 `PlaybackQueueContent` 供手机与桌面共用；播放工作区与歌词/队列背景根据当前封面取色，统一对比度；音量条改用与进度条相同的细 scrubber；队列当前项变化时仍按 entry ID 定位。全量 Flutter 431 项测试、项目级 analyze 和 Linux release 构建通过 | 目前复用手机队列单击播放语义；需补桌面单击选中/双击播放与键盘焦点操作，并对长队列、DPI与真实窗口尺寸验收；阶段仍进行中 |
| 2026-09-23 / `baf0f920` | 播放工作区按可用宽度/高度切换紧凑编排；紧凑状态将封面缩为歌曲摘要栏，歌词/队列保留主要空间。新增 720×460 widget 回归用例；只运行 Dart 格式化和 diff 空白检查，未运行测试、构建或应用 | 紧凑断点的真实 840×560 桌面窗口效果及 DPI 仍待用户手动验收；队列键鼠语义和长队列性能仍未完成 |
| 2026-09-23 / `0007109d` | 桌面队列单击选中、行内显式播放、鼠标可直接拖动的可见手柄；手机单击播放/长按拖动行为保持；队列操作菜单捕获稳定 entry ID，避免延迟菜单动作按旧 index 误操作。新增分离选择与播放的回归用例；仅格式化和 diff 空白检查，未运行测试、构建或应用 | 键盘 Enter/Delete、焦点和快捷键、大队列性能及 Ubuntu 实机交互仍待完成/验收 |
| 2026-09-23 / `41a60336` | 桌面 shell 增加 Space、Ctrl+F、Ctrl+Left/Right 与 Esc；文本编辑和滑杆焦点禁用冲突快捷键。队列焦点内 Enter 播放、Delete 移除选中 entry；队列行可区分键盘 Enter 和 Space。增加搜索输入焦点、Esc 返回、队列 Enter/Delete 用例；仅格式化和 diff 空白检查，未运行测试、构建或应用 | 真实键盘焦点顺序、系统媒体键组合冲突和 Ubuntu 操作待用户验收；长队列与布局/DPI性能仍未验证 |
| 2026-09-23 / `805cd9e4` | 当前项定位不再只按一次固定行高估算，而是测量已构建行的真实 extent，多帧校正后再对目标 entry 执行 `ensureVisible`；当前 entry 或位置变化时清理定位游标。添加 5000 首、当前项位于第 4501 首时仍懒构建并定位的 widget 回归用例。只格式化、diff 空白检查和 Linux release build；未执行测试或启动应用 | 实际 1654/5000 首 release/profile 性能、随机行高与大字体场景、Ubuntu 实际队列滚动仍待用户验收 |
| 2026-09-23 / `5e48d1e5` | 桌面队列行加入鼠标右键菜单：播放、按稳定 entry ID 移除、打开完整曲目操作；菜单显示后按 entry ID 重查当前位置，避免队列变化时旧 index 误操作。新增三种菜单动作的 widget 用例；仅格式化和 diff 空白检查，未运行测试 | 后续最新 Linux bundle 已包含此代码但未启动；Ubuntu 右键和队列变更场景待用户验收 |
| 2026-09-23 / `4046eb32` | 设置增加默认开启、保存在本机的“封面动态背景”选项；关闭时 mini player、队列、桌面工作区和手机全屏播放器使用 Echo 主题 token，避免读取封面调色板。新增偏好读写、静态色和桌面工作区用例；仅格式化/diff 检查和 Linux release build，未运行测试 | Ubuntu 与 Android 的开关即时效果及重启持久化待用户验收；5000 首/1654 首长队列性能仍待测 |
| 2026-09-23 / `b5df9f96`，bundle `1cf593f6` | MainScaffold 首次打开桌面工作区后保留隐藏子树；歌词/队列用按需挂载的 IndexedStack 保留各自 state，隐藏时 TickerMode/Offstage/ExcludeFocus/IgnorePointer 防止不可见输入。新增 panel 切换及返回浏览/重开后队列选中回归用例；Linux release 编译和 `.deb` 组装通过，未运行测试或启动应用 | 用户需验证收起/重开后的队列选中、滚动与歌词手动位置；Android 页面未改变 |
| 2026-09-23 / `07821639` | 队列拖动开始后若 `PlaybackQueueState.revision` 或随机模式改变，则取消旧排序并显示“队列已变化，排序已取消，请重新拖动。”；新增冲突回归用例。未运行测试/分析；Linux release、`.deb` 与 Android arm64 APK 编译成功，均未安装/启动 | 队列变化时不再静默丢弃拖动结果；Android 共享队列使用同一保护提示，原有点击/长按操作不变；真机/键鼠验收待用户执行 |
| 2026-09-23 / `cf131727` | 播放队列为已渲染行锚点记录 index 与 queue revision，当前项定位采样不再对每个锚点线性搜索整个队列；滚动/重建后在帧后清理离屏或过期 `GlobalKey`，避免锚点数量随 5000 首滚动历史增长。未运行 Flutter 测试或 analyze；Linux release/.deb 与 Android arm64 APK 构建成功，签名/ABI/包元数据已核验 | 真实 1654/5000 首 release/profile 内存、帧耗时与当前项定位观感仍待用户采样；播放队列键鼠和 Android 队列交互需用户验收 |
| 2026-09-24 / `f7aa861d` | 增加 5000 首队列先滚到末尾、再把当前曲目切到第 4501 项并重新定位的 widget 回归用例，覆盖离屏锚点回收后继续定位。只格式化和 diff 检查；按用户要求未运行测试、analyze 或应用 | 用例等待用户测试；真实长队列性能采样和 Ubuntu 键鼠交互仍待完成 |
| 2026-09-24 / `16add6b6` | 桌面播放队列标题栏新增“定位当前”按钮，通过请求 ID 重用当前行索引/实测 extent 的渐进定位逻辑；Android 队列没有新增此桌面控件。增加 40 首浏览离开后返回当前项的交互用例与 5000 首重复定位用例；只格式化、diff 检查和 Linux/Android release 编译，按用户要求未运行测试、analyze 或应用 | Linux 队列手动回位已实现，实际滚动手感、低窗口高度按钮空间、键鼠及 Android 回归待用户验收 |
| 2026-09-24 / `82edffab` | EchoSongRow 支持队列桌面可选专辑元数据；队列按可用行宽显示序号、封面、歌名/歌手、专辑与时长，并在窄空间隐藏专辑；移动端不传该选项。新增宽/窄行断点与桌面序号 widget 用例；只格式化、diff 检查和 Linux/Android release 编译，未运行测试、analyze 或应用 | 840×560 下专辑隐藏/标题可读性、宽屏元信息显示、手机队列视觉和无障碍仍待用户手动验收 |
| 2026-09-24 / `87b994e3` | `EchoPressable`/`EchoSongRow` 增加可选双击回调；仅桌面 `PlaybackQueueContent` 使用它调用既有 entry ID 播放命令。新增队列双击播放回归定义；项目版本升至 `1.1.0+2043`。全新 Linux build-dir 编译 `.deb`，Android arm64-only APK 用本机 release 证书签名并核对 ABI、versionCode `2032` 与证书；未运行 Flutter 测试/analyze，也未安装或启动产物 | 桌面队列已满足单击选中、双击播放、Enter/播放按钮播放；手机仍走既有单击播放。用户需运行新增用例并手动验证双击时间/桌面行行为和 Android 回归 |
| 2026-09-24 / `9cbc98fa` | 播放工作区增加可选原生全屏按钮；Linux/Windows 桌面监听全屏进出，Esc 优先退出全屏；`DesktopWindowStateService` 暂停记录全屏尺寸，退出后恢复普通尺寸写入。宽屏 Android 不显示此原生控制。新增按钮切换、Esc 退出优先级、正常尺寸保护和 Android 隐藏入口回归定义；项目版本升至 `1.1.0+2044`。Linux amd64 `.deb` 与本机签名 Android arm64-only APK 编译、包信息/ABI/证书校验通过；测试/analyze 未运行，产物未安装或启动 | 全屏继续复用同一播放工作区；当前保留 Echo 自绘标题栏，GNOME 全屏行为、退出后窗口几何和 Android 回归待用户手测 |
